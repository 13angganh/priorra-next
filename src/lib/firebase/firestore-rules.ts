/**
 * This file is documentation/reference only — the *actual*
 * Firestore Security Rules live in /firestore.rules at the project
 * root, per Architecture Proposal section 2 ("Catatan penempatan"
 * is implied by the file's own docstring here — the enforced rules
 * are never duplicated into application source).
 *
 * Populated in Development Phase #26 alongside firestore.rules.
 */

export {};
